#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT_DIR"
./{{CLI_NAME}} preflight
./{{CLI_NAME}} inventory scan
./{{CLI_NAME}} inventory report
printf '\nDone. You can close this window.\n'
read -r -p "Press Return to close..." _
